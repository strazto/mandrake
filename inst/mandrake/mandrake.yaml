# Generated by roxygen2: do not edit by hand

mpg:
  direction: !<list> out
  name: mpg
  aliases: !<list> []
  body: |2-

    Numeric.
    `mpg` or "Miles Per Gallon" is how many miles you can drive on
    a gallon of fuel.
  rd: |-
    Numeric.
    \code{mpg} or "Miles Per Gallon" is how many miles you can drive on
    a gallon of fuel.
  html: !<list>
  - Numeric.
  - <code>mpg</code> or "Miles Per Gallon" is how many miles you can drive on
  - a gallon of fuel.
  topic: mtcars_dataset
  package: mandrake
  rd_ref: \code{\link[mandrake:mtcars_dataset]{mandrake::mtcars_dataset}}
  html_ref: <code>mandrake::mtcars_dataset</code>
cyl:
  direction: !<list> out
  name: cyl
  aliases: !<list> []
  body: |2-

    Integer
    Number of cylinders
  rd: |-
    Integer
    Number of cylinders
  html: !<list>
  - Integer
  - Number of cylinders
  topic: mtcars_dataset
  package: mandrake
  rd_ref: \code{\link[mandrake:mtcars_dataset]{mandrake::mtcars_dataset}}
  html_ref: <code>mandrake::mtcars_dataset</code>
disp:
  direction: !<list> out
  name: disp
  aliases: !<list> []
  body: |2-

    Integer.
    I literally don't know what this means
  rd: |-
    Integer.
    I literally don't know what this means
  html: !<list>
  - Integer.
  - I literally don't know what this means
  topic: mtcars_dataset
  package: mandrake
  rd_ref: \code{\link[mandrake:mtcars_dataset]{mandrake::mtcars_dataset}}
  html_ref: <code>mandrake::mtcars_dataset</code>
hp:
  direction: !<list> out
  name: hp
  aliases: !<list> []
  body: |2-

    Integer
    Horsepower is a measure of the number of miniature horses this car
    can fit inside itself, which run on a treadmill that make it go fast.
  rd: |-
    Integer
    Horsepower is a measure of the number of miniature horses this car
    can fit inside itself, which run on a treadmill that make it go fast.
  html: !<list>
  - Integer
  - Horsepower is a measure of the number of miniature horses this car
  - can fit inside itself, which run on a treadmill that make it go fast.
  topic: mtcars_dataset
  package: mandrake
  rd_ref: \code{\link[mandrake:mtcars_dataset]{mandrake::mtcars_dataset}}
  html_ref: <code>mandrake::mtcars_dataset</code>
drat:
  direction: !<list> out
  name: drat
  aliases: !<list> []
  body: |2-

    Numeric
    This is a word you say when something doesn't go the way you hoped.
    **Drat!**
  rd: |-
    Numeric
    This is a word you say when something doesn't go the way you hoped.
    \strong{Drat!}
  html: !<list>
  - Numeric
  - This is a word you say when something doesn't go the way you hoped.
  - <strong>Drat!</strong>
  topic: mtcars_dataset
  package: mandrake
  rd_ref: \code{\link[mandrake:mtcars_dataset]{mandrake::mtcars_dataset}}
  html_ref: <code>mandrake::mtcars_dataset</code>
wt:
  direction: !<list> out
  name: wt
  aliases: !<list> []
  body: |2-

    Numeric
    Stands for "What the..." - An exclamation of surprise!

    Or **weight**, maybe.
  rd: |-
    Numeric
    Stands for "What the..." - An exclamation of surprise!

    Or \strong{weight}, maybe.
  html: !<list>
  - Numeric
  - Stands for "What the..." - An exclamation of surprise!
  - ''
  - Or <strong>weight</strong>, maybe.
  topic: mtcars_dataset
  package: mandrake
  rd_ref: \code{\link[mandrake:mtcars_dataset]{mandrake::mtcars_dataset}}
  html_ref: <code>mandrake::mtcars_dataset</code>
qsec:
  direction: !<list> out
  name: qsec
  aliases: !<list> []
  body: |2-

    Numeric.
    Number of seconds you need to line up in a queue in order to buy this car.
  rd: |-
    Numeric.
    Number of seconds you need to line up in a queue in order to buy this car.
  html: !<list>
  - Numeric.
  - Number of seconds you need to line up in a queue in order to buy this car.
  topic: mtcars_dataset
  package: mandrake
  rd_ref: \code{\link[mandrake:mtcars_dataset]{mandrake::mtcars_dataset}}
  html_ref: <code>mandrake::mtcars_dataset</code>
vs:
  direction: !<list> out
  name: vs
  aliases: !<list> []
  body: |2-

    Logical, Does the car ever compete?
  rd: Logical, Does the car ever compete?
  html: !<list> Logical, Does the car ever compete?
  topic: mtcars_dataset
  package: mandrake
  rd_ref: \code{\link[mandrake:mtcars_dataset]{mandrake::mtcars_dataset}}
  html_ref: <code>mandrake::mtcars_dataset</code>
am:
  direction: !<list> out
  name: am
  aliases: !<list> []
  body: |2-

    Logical, is this car okay to use in the morning, or should you save it for
    after midday?
  rd: |-
    Logical, is this car okay to use in the morning, or should you save it for
    after midday?
  html: !<list>
  - Logical, is this car okay to use in the morning, or should you save it for
  - after midday?
  topic: mtcars_dataset
  package: mandrake
  rd_ref: \code{\link[mandrake:mtcars_dataset]{mandrake::mtcars_dataset}}
  html_ref: <code>mandrake::mtcars_dataset</code>
gear:
  direction: !<list> out
  name: gear
  aliases: !<list> []
  body: |2-

    Integer,

    How many different gears transmissions does this car have.
  rd: |-
    Integer,

    How many different gears transmissions does this car have.
  html: !<list>
  - Integer,
  - ''
  - How many different gears transmissions does this car have.
  topic: mtcars_dataset
  package: mandrake
  rd_ref: \code{\link[mandrake:mtcars_dataset]{mandrake::mtcars_dataset}}
  html_ref: <code>mandrake::mtcars_dataset</code>
carb:
  direction: !<list> out
  name: carb
  aliases: !<list> []
  body: |2-

    Integer, Number of grams of carbohydrates contained in this car.
    Typically pretty low, as cars are not particularly nutritious.
  rd: |-
    Integer, Number of grams of carbohydrates contained in this car.
    Typically pretty low, as cars are not particularly nutritious.
  html: !<list>
  - Integer, Number of grams of carbohydrates contained in this car.
  - Typically pretty low, as cars are not particularly nutritious.
  topic: mtcars_dataset
  package: mandrake
  rd_ref: \code{\link[mandrake:mtcars_dataset]{mandrake::mtcars_dataset}}
  html_ref: <code>mandrake::mtcars_dataset</code>

